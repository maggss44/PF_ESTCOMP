version: '3.7'
services:
  db:
    image: "postgres"
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - pg_api:/var/lib/postgresql/data
    ports:
      - 5432:5432
  web:
    build: ./api
    ports:
      - 8080:8080
  shiny: 
    build: ./shiny
    ports:
      - 5000:4999
volumes:
  pg_api: